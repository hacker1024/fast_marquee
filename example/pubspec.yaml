name: fast_marquee_example
description: A fast_marquee example.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  fast_marquee:
    path: '../'

dev_dependencies:
  lint: ^1.5.3

flutter:
  uses-material-design: true